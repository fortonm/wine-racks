/*global define*/
define([
	'jquery',
	'backbone',
	'collections/wines',
	'common'
], function (jQuery, Backbone, Wines, Common) {
	'use strict';

	var Workspace = Backbone.Router.extend({
		
	});

	return Workspace;
});